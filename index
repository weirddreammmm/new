<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>블랙박스 AI 예측 게임</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            text-align: center;
            background-color: #f5f5f5;
            padding: 20px;
        }
        button {
            margin: 10px;
            padding: 10px 20px;
            font-size: 16px;
        }
        #result {
            margin-top: 20px;
            font-size: 18px;
            color: #333;
        }
    </style>
</head>
<body>
    <h1>블랙박스 AI 예측 게임</h1>
    <p>이번엔 AI가 인간으로 판단할까요, 고양이로 판단할까요?</p>

    <!-- 예측 버튼 -->
    <button onclick="predict('인간')">인간</button>
    <button onclick="predict('고양이')">고양이</button>

    <!-- 결과를 표시할 영역 -->
    <div id="result"></div>

    <script>
        // 스테이지 설정: AI의 엉뚱한 판단 결과와 이유
        const stages = [
            { correctAnswer: '인간', reason: '그림자 각도가 인간과 유사해 \'인간\'으로 판단했습니다.' },
            { correctAnswer: '인간', reason: '발소리 반사 때문에 \'인간\'으로 판단했습니다.' },
            { correctAnswer: '인간', reason: '벽의 거리 때문에 \'인간\'으로 판단했습니다.' }
        ];
        let currentStage = 0;

        function predict(userPrediction) {
            const aiDecision = stages[currentStage].correctAnswer;
            const reason = stages[currentStage].reason;
            
            if (userPrediction === aiDecision) {
                document.getElementById('result').innerText = 
                    `정답입니다! 하지만 AI는 ${reason}`;
            } else {
                document.getElementById('result').innerText = 
                    `오답입니다! AI는 엉뚱한 이유로 ${aiDecision}이라고 판단했습니다: ${reason}`;
            }
            
            // 다음 스테이지로 진행
            currentStage = (currentStage + 1) % stages.length;
        }
    </script>
</body>
</html>
